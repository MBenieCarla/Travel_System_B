# Travel Booking System

Django project initialized by Member 1 (Benie)

## Setup Complete
- Django project created
- `users` app added
- Templates and static dirs configured

## How to Run
1. Clone the repo
2. Create a virtual environment and install Django
3. Run the server:

   python manage.py runserver
